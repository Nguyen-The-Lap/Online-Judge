# Frontend environment example for ByteForge Judge
# Copy to `.env.local` before running `npm run dev`

VITE_API_BASE_URL=http://localhost:8080/api
VITE_APP_NAME=ByteForge Judge
VITE_DEFAULT_THEME=dark

